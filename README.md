Input the `start` and `end`, and it will output digits from the `start`th to the `end`th to build/output.txt .

e.g.
```shell
> make
> .\build\pi_hex.exe
start=1
end=10
> cat .\build\output.txt
243f6a8885
```
